# 章节摘要（Chapter Summaries）

> 自动渲染：来自 `state/chapter_summaries.json`，每次 `apply_delta` 后由脚本重写。
> 直接编辑此文件不会持久化；要改请改 json 或通过 settler delta。

| chapter | title | characters | events | stateChanges | hookActivity | mood | chapterType |
|---|---|---|---|---|---|---|---|
